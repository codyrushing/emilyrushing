<nav class="top-nav">
    <div class="top-nav-logo">
      <a class="top-nav-logo" href="{{@site.url}}">
        <span>Emily</span>
        <span>Rushing</span>
      </a>
    </div>
    <div class="nav-links">
      {{#if @site.navigation}}
          {{navigation}}
      {{/if}}
      <div class="nav-right">
        <a href="#site-nav-modal" rel="modal" class="menu-link">{{> icons/menu }}</a>
      </div>
    </div>
  {{!-- {{#if @site.navigation}}
      {{navigation}}
  {{/if}} --}}
</nav>
